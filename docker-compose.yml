version: "3.9"

services:
  dev:
    build: .
    image: feature-toggle-poc:latest
    env_file:
      - config/dev.env

  qa:
    image: feature-toggle-poc:latest
    env_file:
      - config/qa.env

  prod:
    image: feature-toggle-poc:latest
    env_file:
      - config/prod.env
